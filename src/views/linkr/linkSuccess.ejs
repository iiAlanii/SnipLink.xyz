<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Link Success</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" />
    <style>
        body {
            font-family: 'Arial', sans-serif;
            margin: 0;
            padding: 0;
            background: #f4f4f4;
            display: flex;
            align-items: center;
            justify-content: center;
            min-height: 100vh;
            position: relative;
        }

        .container {
            text-align: center;
            padding: 30px;
            border-radius: 15px;
            background-color: #fff;
            box-shadow: 0 0 20px rgba(0, 0, 0, 0.2);
            max-width: 600px;
            width: 90%;
            margin: 20px;
            position: relative;
            z-index: 1;
            animation: pulseAnimation 1s ease-in-out forwards;
            opacity: 0;
        }

        h1 {
            color: #2c3e50;
            font-size: 2.5em;
            margin-bottom: 20px;
        }

        p {
            margin-bottom: 20px;
            font-size: 1.2em;
            color: #34495e;
            line-height: 1.6;
        }

        .success-icon {
            color: #2ecc71;
            font-size: 3em;
            margin-bottom: 20px;
        }

        .back-btn {
            padding: 10px 20px;
            background-color: #3498db;
            color: #fff;
            text-decoration: none;
            border-radius: 5px;
            transition: background-color 0.3s ease;
            font-size: 1.2em;
            cursor: pointer;
        }

        .back-btn:hover {
            background-color: #2980b9;
        }

        .confetti {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            pointer-events: none;
            z-index: 0;
            overflow: hidden;
        }

        .confetti div {
            position: absolute;
            width: 10px;
            height: 10px;
            background: #f1c40f;
            border-radius: 50%;
            opacity: 0;
            animation: confettiAnimation 4s linear infinite;
        }

        @keyframes confettiAnimation {
            0% {
                transform: translateY(0) rotate(0);
                opacity: 1;
            }
            100% {
                transform: translateY(100vh) rotate(720deg);
                opacity: 0;
            }
        }

        @keyframes pulseAnimation {
            0% {
                transform: scale(0);
                opacity: 0;
            }
            100% {
                transform: scale(1);
                opacity: 1;
            }
        }
    </style>
</head>
<body>
<div class="container">
    <i class="fas fa-check-circle success-icon"></i>
    <h1>Link Success!</h1>
    <p>Your r.mtdv.me link has been successfully linked to SnipLink.xyz!</p>
    <p>You can now access detailed analytics for your links, allowing you to track performance and engagement. Stay tuned for more features as we continue to unlock additional capabilities for you!</p>
    <div class="useful-links">
        <p>To see link analytics, <a href="https://sniplink.xyz/analytics/<%= shortenedUrl %>">click here</a>.</p>
        <p>To visit the dashboard, <a href="/dashboard">click here</a>.</p>
    </div>
    <a href="/" class="back-btn">Back to Home</a>
</div>

<div class="confetti"></div>


<script>
    const confettiContainer = document.querySelector('.confetti');
    const numConfetti = 200;
    const colors = ['#f1c40f', '#e74c3c', '#3498db', '#2ecc71', '#9b59b6'];
    for (let i = 0; i < numConfetti; i++) {
        const confetti = document.createElement('div');
        confetti.style.left = `${Math.random() * 100}%`;
        confetti.style.animationDelay = `${Math.random() * 4}s`;
        confetti.style.backgroundColor = colors[Math.floor(Math.random() * colors.length)];
        confettiContainer.appendChild(confetti);
    }
</script>

</body>
</html>
